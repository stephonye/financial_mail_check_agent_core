# 数据库配置 (直接连接)
DATABASE_URL=postgresql://username:password@localhost:5432/financial_emails_db

# MCP数据库配置 (可选)
MCP_DATABASE_URL=postgresql://username:password@mcp-server:5432/financial_emails_db
MCP_SERVER_URL=http://localhost:8000
MCP_ENABLED=false

# 汇率API配置（可选）
EXCHANGE_RATE_API_KEY=your_exchangerate_api_key_here

# Gmail配置（已在credentials.json中配置）

# 说明：
# 1. 复制此文件为 .env
# 2. 根据实际环境修改配置
# 3. 设置 MCP_ENABLED=true 启用MCP连接
# 4. 不要将真实的 .env 文件提交到版本控制